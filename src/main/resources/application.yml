camel:
  server-port: 5001
  springboot:
    main-run-controller: true
  dataformat:
    json-jackson:
      auto-discover-object-mapper: true

bpmn:
  flows:
    party-lookup: "PayeePartyLookup-{tenant}"
    quote: "PayeeQuoteTransfer-{tenant}"
    transaction-request: "PayerTransactionRequest-{tenant}"

zeebe:
  client:
    max-execution-threads: 50
    evenly-allocated-max-jobs: 1000
    poll-interval: 10
#    max-execution-threads: 100
#    number-of-workers: 15
#    evenly-allocated-max-jobs: "#{${zeebe.client.max-execution-threads} / ${zeebe.client.number-of-workers}}"
  broker:
    #contactpoint: "ph-ee-zeebe:26500"
    contactpoint: "zeebe-gateway.mifos.gazelle.test:80"

connector:
  ilp-secret: h4on38bsDjKiat2783gnklgafikmeuu5123kpobb7jm99


mojaloop:
  enabled: true
  perf-mode: false
  perf-resp-delay: 100

switch:
  quotes-host: ""
  quote-service: quoting-service.fred
  als-host: http://vnextadmin.mifos.gazelle.test/_interop
  account-lookup-service: http://vnextadmin.mifos.gazelle.test/_interop
  transfers-host: ""
  transfer-service: ml-api-adapter.fred
  transactions-host: ""
  transaction-request-service: transaction-request-service.fred
  oracle-host: ""

dfspids: "greenbank,bluebank"

# logging:
#   level:
#     root: DEBUG
#     org:
#       apache:
#         camel: DEBUG



server:
  port: 8080
management:
  endpoint:
    health:
      enabled: true
      probes:
        enabled: true
      liveness:
        enabled: true
      readiness:
        enabled: true

security:
  jws:
    enable: false
    response:
      enable: false
